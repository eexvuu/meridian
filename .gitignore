# Dependencies
node_modules/

# Environment & secrets — keep all real secrets here (gitignored)
.env
.env.*
!.env.example
.envrypt

# User config is tracked (tuning params only — no secrets).
# Secrets (RPC URL, wallet key, API keys) live in .env.

# Runtime state (position tracking, lessons — user-specific)
state.json
lessons.json
smart-wallets.json
pool-memory.json
token-blacklist.json
strategy-library.json
decision-log.json
hivemind-cache.json
signal-weights.json

# Paper trading state (DRY_RUN-aware mirrors)
paper-state.json
paper-lessons.json
paper-pool-memory.json
paper-signal-weights.json

# Debug dumps and temp working files (may contain prompts / secrets)
last-400-request.json
screener-tools.tmp.json
.claude/settings.local.json

# Logs
logs/

# OS
.DS_Store

# Dev context
context.md
CONTEXT.md
dev-blocklist.json
docs/

# Discord listener queue
discord-signals.json

# Playwright MCP browser logs
.playwright-mcp/

# Claude Code runtime lock
.claude/scheduled_tasks.lock
